import React from 'react';import { View, TouchableOpacity, Text } from 'react-native';const TabSwitcher = ({ activeTab, onTabChange }) => {  const tabs = [    { id: 'pending', label: 'Pending' },    { id: 'completed', label: 'Completed' },    { id: 'new', label: '+ New Task' },  ];  return (    <View style={{ paddingHorizontal: 20, paddingVertical: 16 }}>      <View style={{         flexDirection: 'row',         backgroundColor: '#f1f5f9',         borderRadius: 12,         padding: 4,      }}>        {tabs.map((tab) => {          const isActive = activeTab === tab.id;          const isNew = tab.id === 'new';          return (            <TouchableOpacity              key={tab.id}              style={{                flex: 1,                paddingVertical: 10,                borderRadius: 8,                alignItems: 'center',                justifyContent: 'center',                backgroundColor: isActive ? '#ffffff' : 'transparent',                borderWidth: isNew && !isActive ? 1.5 : 0,                borderColor: isNew && !isActive ? '#6366f1' : 'transparent',              }}              onPress={() => onTabChange(tab.id)}              activeOpacity={0.7}            >              <Text style={{                fontSize: 14,                fontWeight: isActive ? '700' : '600',                color: isActive ? '#6366f1' : (isNew ? '#6366f1' : '#64748b'),              }}>                {tab.label}              </Text>            </TouchableOpacity>          );        })}      </View>    </View>  );};export default TabSwitcher;